.venv
.env
*.egg-info/
__pycache__/
*.py[cod]
*.log
.DS_Store
.pytest_cache/
# insightface model pack (~300 MB) — downloaded on first boot, never committed
models/
# ...but the anti-spoof models are 3.3 MB and DO ship: converting them needs
# PyTorch, which a server should not have to install to deploy.
!models/antispoof/
# local calibration images — these are biometric data, keep them out of git
dataset/
